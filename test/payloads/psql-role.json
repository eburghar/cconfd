{
    "db_name": "postgresql",
    "creation_statements": ["CREATE ROLE '{{name}}' WITH CREATEDB PASSWORD '{{password}}'", "GRANT SELECT ON *.* TO '{{name}}'"],
    "default_ttl": "1h",
    "max_ttl": "24h"
}